# fmt: off
[
    {
        "name": "free_space",
        "getval": r"^Mem:\s+\d+\s+\d+\s+(?P<total_bytes>\d+)\s+\d+\s+(?P<free_bytes>\d+)\s+",
        "result": {"free_bytes": "{{ free_bytes }}"}
    }
]
# fmt: on
